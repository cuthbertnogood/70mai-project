# macOS
.DS_Store
.AppleDouble
.LSOverride

# Video and media (large files — keep locally, not in git)
video/
*.mp4
*.MP4
*.mov
*.avi
*.mkv

# GPS data from dashcam SD card
GPSData*.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# YouTube OAuth (keep locally)
youtube_credentials.json
youtube_token.json
.config/70mai/

# Publish state (under video/ but explicit if copied)
*.state.json

# Local run logs
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# Cursor hook runtime state (per-chat edit counters)
.cursor/hooks/state/

# Cursor chat history (may contain pasted secrets — never commit)
.specstory/
